#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""Task_01"""


def get_matches(*players):
    """function takes argument which is a list of player names
    and provides and game match up for the list"""
    #new_list= tuple(list(players))
    #new_list = []
    for x in (players):
        #new_ = list(zip(players)
        #print(x + str(index))
        #players.append(x)
        for y in enumerate(players):
            #y = list(zip(y)
            #new_list.append(y)
            #if x in y:
                #pass
           # if y in x:
               # pass
           # else:
                #return y
            return x, y
